#include <iostream>
#include "multiply_numbers.h"

int main(){
    int a = 5;
    int b = 10;
    std::cout << "The result of multiplying a = " << a << " and b = " << b << " is " << multiply_integers(a, b);
    return 0;
}